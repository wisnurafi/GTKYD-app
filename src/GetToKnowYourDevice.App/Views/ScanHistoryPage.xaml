<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GetToKnowYourDevice.App.Views.ScanHistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Scan History" Style="{StaticResource TitleTextBlockStyle}" />

        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" PlaceholderText="Search history..."
                     Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     KeyUp="Search_KeyUp" />
            <Button Grid.Column="1" Content="Refresh" Command="{x:Bind ViewModel.LoadCommand}" />
        </Grid>

        <TextBlock Grid.Row="2" Text="No saved scans yet."
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />

        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Records, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedRecord, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="12" Padding="0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock FontWeight="SemiBold">
                                <Run Text="{Binding ScanType}" /><Run Text=" - " />
                                <Run Text="{Binding ScanName}" />
                            </TextBlock>
                            <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                <Run Text="Health:" /><Run Text="{Binding HealthScore}" />
                                <Run Text="  Status:" /><Run Text="{Binding ScanStatus}" />
                            </TextBlock>
                        </StackPanel>
                        <Button Grid.Column="1" Content="Open" Click="Open_Click" Tag="{Binding}" />
                        <Button Grid.Column="2" Content="Pin" Click="Pin_Click" Tag="{Binding}" />
                        <Button Grid.Column="3" Content="Delete" Click="Delete_Click" Tag="{Binding}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
