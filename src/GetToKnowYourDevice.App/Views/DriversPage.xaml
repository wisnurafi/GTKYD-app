<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GetToKnowYourDevice.App.Views.DriversPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Drivers" Style="{StaticResource TitleTextBlockStyle}" />

        <Grid Grid.Row="1" ColumnSpacing="8"
              Visibility="{x:Bind ViewModel.HasReport, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" PlaceholderText="Search drivers..."
                     Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <ComboBox Grid.Column="1" x:Name="FilterCombo" SelectedIndex="0" SelectionChanged="FilterCombo_SelectionChanged">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Signed" />
                <ComboBoxItem Content="Unsigned" />
                <ComboBoxItem Content="Error" />
                <ComboBoxItem Content="Microsoft" />
                <ComboBoxItem Content="Third-party" />
                <ComboBoxItem Content="Old candidate" />
            </ComboBox>
            <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="8,0">
                <Run Text="{x:Bind ViewModel.FilteredCount, Mode=OneWay}" />
                <Run Text="/" />
                <Run Text="{x:Bind ViewModel.TotalCount, Mode=OneWay}" />
            </TextBlock>
        </Grid>

        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Drivers, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedDriver, Mode=TwoWay}"
                  Visibility="{x:Bind ViewModel.HasReport, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,4">
                        <TextBlock Text="{Binding DeviceName}" FontWeight="SemiBold" />
                        <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="{Binding DeviceClass}" /><Run Text=" | " />
                            <Run Text="{Binding DriverProvider}" /><Run Text=" | v" />
                            <Run Text="{Binding DriverVersion}" /><Run Text=" | Signed: " />
                            <Run Text="{Binding IsSigned}" />
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock Grid.Row="2" Text="No scan yet. Run a scan to view drivers."
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Visibility="{x:Bind ViewModel.HasReport, Mode=OneWay, Converter={StaticResource BoolToVisibility}, ConverterParameter=invert}" />
    </Grid>
</Page>
